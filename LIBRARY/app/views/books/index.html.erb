<%= stylesheet_link_tag "application.css" %>
<h1>Books</h1>

<%= link_to "Add a New Book", new_book_path %>

<div id="books">
  <% @books.each do |book| %>
    <div>
       <%= link_to book.title, book %>
    </div>
  <% end %>
</div>